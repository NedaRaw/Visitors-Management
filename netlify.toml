[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client-side hash routes (#/admin, #/visitor/:id)
# never hit a server 404. (We use hash routing, so this is a safety net.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
